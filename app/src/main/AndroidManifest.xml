<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practic"
        tools:ignore="ExtraText"
        tools:targetApi="31">

        <provider
            android:name="com.example.practic.themes.sdk_4.interview_questions.content_provider.NotesProvider"
            android:authorities="com.example.practic.themes.sdk_4.interview_questions.content_provider.notesprovider"
            android:enabled="true"
            android:exported="false" />


        <provider
            android:name=".themes.sdk_4.interview_questions.file_provider.MyFileProvider"
            android:authorities="com.example.fileproviderdemo.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


        <activity
            android:name=".themes.sdk_4.interview_questions.file_provider.FileProviderActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>