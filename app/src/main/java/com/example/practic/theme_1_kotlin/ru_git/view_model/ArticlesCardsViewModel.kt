package com.example.practic.theme_1_kotlin.ru_git.view_model

/*
class ArticlesCardsViewModel : BaseViewModel() {

    fun loadArticles() {
        fetchData(
            action = { getArticlesUseCase.execute() },
            onSuccess = { articles ->
                // Обработать загруженные статьи
            },
            onError = { error ->
                // Логика обработки ошибок
            }
        )
    }
}
*/
